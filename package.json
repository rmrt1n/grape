{
  "name": "grape",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "format": "turbo run format",
    "lint": "turbo run lint"
  },
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.10.7"
  }
}
